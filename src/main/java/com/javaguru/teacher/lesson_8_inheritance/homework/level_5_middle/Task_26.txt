Создание вспомогательныго кода для иерархии Shape (геометрическая фигура).

В класе ShapeUtil реализуйте метод:

    double calculatePerimeter(Shape[] shapes);

Этот метод должен расчитывать общий периметр
геометрических фигур из переданного в качестве параметра массива.